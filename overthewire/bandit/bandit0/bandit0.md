password
bandit0

En este nivel simplemente debemos conectarnos a la red de overthewire con el usuario correspondiente en este caso el usuario `bandit0`

1. Nos conectamos por ssh
```
ssh@bandit0.overthewire.org
```
2. Ingresamos la password, en este caso "bandit0"
```
bandit0
```
Con eso podemos avanzar al siguiente nivel
